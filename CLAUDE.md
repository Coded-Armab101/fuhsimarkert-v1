@AGENTS.md
@PROJECT_HISTORY.md

Before starting work, read `PROJECT_HISTORY.md` — it records where the last
session stopped, the real Supabase schema (which differs from what the code
implies in several places), and the open backlog. When you finish a piece of
work, append a dated entry to its Session Log.
